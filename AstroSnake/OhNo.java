import greenfoot.*;  // (World, Actor, GreenfootImage, Greenfoot and MouseInfo)

/**
 * Write a description of class OhNo here.
 * 
 * @author (your name) 
 * @version (a version number or a date)
 */
public class OhNo extends Actor
{
    public OhNo()
    {
        GreenfootImage img = new GreenfootImage(40*25, 30*25);
        img.setColor(Color.LIGHT_GRAY);
        img.setTransparency(150);
        img.fill();
        setImage(img);
    }
    /**
     * Act - do whatever the OhNo wants to do. This method is called whenever
     * the 'Act' or 'Run' button gets pressed in the environment.
     */
    public void act()
    {
        // Add your action code here.
    }
}
